pre-commit:
  parallel: true
  commands:
    prettier:
      glob: '*.{ts,json,yaml,yml,md}'
      run: $(npm bin)/prettier --loglevel warn --check {staged_files}
    eslint:
      glob: '*.{ts}'
      run: $(npm bin)/eslint {staged_files}
    tsc:
      run: $(npm bin)/tsc -p ./tsconfig.json
